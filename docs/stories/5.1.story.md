# Story 5.1: Integração Stripe (Subscriptions + Metering)

**Status:** Draft | **Sprint:** 9 | **Complexity:** 10 pts

Como um **founder**, Quero **aceitar pagamentos Stripe com subscriptions**, Para que **monetize o produto**.

**AC:**
- [ ] Stripe account configurado, API keys em env vars
- [ ] Endpoint `/api/billing/create-checkout-session` retorna URL
- [ ] Webhook `/api/webhooks/stripe` processa eventos
- [ ] Customer vinculado a `organization_id` em `billing_subscriptions`
- [ ] Testes: criar subscription → organization tem `status = 'active'`

**Files:** `app/api/billing/route.ts`, `lib/stripe/stripe-service.ts`, `tests/billing/stripe.test.ts`
