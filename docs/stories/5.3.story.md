# Story 5.3: Usage Metering (Mensagens, Contatos, Flows)

**Status:** Draft | **Sprint:** 9-10 | **Complexity:** 8 pts

Como um **gerente**, Quero **saber exatamente quanto estou usando e o que vou pagar**, Para que **controle custos**.

**AC:**
- [ ] Tabela `usage_events` registra org_id, metric, quantity, timestamp
- [ ] Função PL/pgSQL `increment_usage(org_id, metric, qty)` incrementa
- [ ] Chamado em: `send_message()`, `create_contact()`, `execute_workflow()`
- [ ] Dashboard `/dashboard/usage` mostra gráfico + breakdown
- [ ] Stripe metered billing reporta consumo via `/api/metering/report`

**Files:** `lib/billing/usage-tracker.ts`, `app/dashboard/usage/page.tsx`, `tests/billing/metering.test.ts`
