# Story 2.4: Deal Creation via WhatsApp (Keyword Trigger)

**Status:** Ready
**Sprint:** 4
**Epic:** Epic 2 - Integração WhatsApp↔CRM
**Story ID:** 2.4
**Complexity:** 8 pts

---

## Description

Como um **SDR**,
Quero **que uma mensagem WhatsApp com keyword específica crie automaticamente um deal**,
Para que **não perca leads qualificados**.

---

## Acceptance Criteria

- [ ] **AC1:** Admin pode configurar keywords por org (ex: "quero orçamento", "preciso de demo")
- [ ] **AC2:** Função PL/pgSQL `process_inbound_message` verifica keywords
- [ ] **AC3:** Se match: cria Deal com: `contact_id`, `board_id` (padrão "Web Leads"), `stage_id` (primeiro stage "Novo"), `name` (auto-gerado), `deal_value` (default null)
- [ ] **AC4:** Deal criado dispara notificação para assign (Slack, email)
- [ ] **AC5:** Testes: mensagem com keyword cria deal, sem keyword não cria

---

## Scope

### In Scope
- Keyword configuration per org
- Inbound message keyword matching
- Automatic deal creation
- Deal default values setup
- Notification trigger on creation
- Tests for keyword matching + deal creation

### Out of Scope
- Slack/Email notification implementation (separate story)
- Deal assignment logic (Epic 4)
- UI for keyword management (Epic 3)

---

## File List

**To Create:**
- [ ] `lib/bridge/keyword-matcher.ts`
- [ ] `lib/bridge/deal-creator.ts`
- [ ] `app/api/keywords/route.ts` (GET/POST keyword config)
- [ ] `supabase/functions/process_inbound_message.sql`
- [ ] `tests/bridge/keyword-matcher.test.ts`

**To Modify:**
- [ ] `lib/db/schema.ts` (add keywords table if not exists)

---

## Notes for Developer

- Keywords table: org_id, keyword (text), board_id, stage_id
- Matching: case-insensitive, partial match (use `ILIKE` in SQL)
- Deal name template: "Lead from WhatsApp - {contact_name} - {date}"
- Notification: emit event that Epic 5 (Notifications) can consume
- PL/pgSQL function: called from webhook handler (Story 2.2)
- Performance: cache keywords in memory (TTL 5min) to avoid DB hit per message

---

## Dependencies

**Blocked by:**
- Story 2.1 (Bridge Service Architecture)
- Story 2.2 (Contact sync)
- Story 2.3 (Activity sync)

---

## Change Log

| Data | Autor | Ação |
|------|-------|------|
| 2026-03-01 | River (@sm) | Story criada |

---

## Dev Progress

- [ ] Keyword configuration API working
- [ ] Keyword matcher logic tested
- [ ] Deal creation tested
- [ ] Notification event emitted
- [ ] Tests passing (keyword match = deal, no match = no deal)
- [ ] Ready for @po validation
